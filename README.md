# eqbuddy-telemetry

The backend for EQBuddy Evolved's opt-in heartbeat: a Cloudflare Worker plus D1.
It is public so anyone can read exactly what it stores.

The implementation is under review in the first pull request. Nothing is
deployed, and EQBuddy sends no telemetry.
